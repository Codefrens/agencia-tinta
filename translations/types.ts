export type Translations = {
  common: {
    navigationLinks: {
      agency: string;
      porfolio: string;
      foodar: string;
      contact: string;
    };
  };
  homePage: {
    hero: {
      title: string;
      subtitle: string;
      reelLabel: string;
    };
  };
};
